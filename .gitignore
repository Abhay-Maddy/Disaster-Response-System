# Environment files
.env
*.local

# Editor directories
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Root-level node_modules (the real one is inside frontend/)
node_modules

# The /docs folder is no longer used (build now outputs to repo root)
docs/

# DO NOT ignore the following — they are the built site served by GitHub Pages:
# index.html, login.html, register.html, admin.html, admin-register.html
# assets/  favicon.svg  icons.svg
